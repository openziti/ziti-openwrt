# Build outputs
build/
*.ipk
.DS_Store
Thumbs.db

# Locally-built ziti router binaries (~70 MB each). Produced by
# tools/build-ziti-router.sh into package/ziti-router/files/binaries/.
# Don't commit -- consumers either build them themselves or fetch them
# from a feed.
package/ziti-router/files/binaries/

# Temporary parse-check / sanity scripts
tools/.cache/

# One-time enrollment tokens and local identity/service bodies -- never commit
*.jwt
/internet-intercept.json
/internet-host.json

# Local agent / review tooling scratch (not part of the shipped repo)
/.claude/
/.mercurius/
/mercurius.yaml
